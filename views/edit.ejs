<!-- edit.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans bg-zinc-800 text-white">

    <!-- nav bar -->
    <div class="w-full px-4 sm:px-6 md:px-8 lg:px-10 py-3 flex items-center justify-between bg-zinc-800 text-white">
        <a href="/feed" class="text-lg font-semibold">Pinterest</a>
        <div class="flex items-center">
                <a href="/search"><input class="p-2 rounded-lg w-28 md:w-40 lg:w-60 ml-2 md:ml-4 mr-2 md:mr-4" type="text" placeholder="Search"></a>
                <div class="flex gap-4 items-center">
                <a href="/profile">Profile</a>
                <a href="/feed">Feed</a>
                <a class="px-3 py-2 bg-red-700 text-white rounded-lg text-sm md:text-base" href="/logout">Log Out</a>
            </div>
        </div>
    </div>

    <div class="w-full min-h-screen bg-zinc-800 text-white pt-1 px-4 sm:px-6 md:px-8 lg:px-10">
        <form action="/edit" method="post" class="max-w-md mx-auto mt-10 text-black">
            <!-- Edit form fields go here -->
            <label for="name" class="block text-sm font-medium text-zinc-300">Name</label>
            <input type="text" id="name" name="name" value="<%= user.name %>" class="mt-1 p-2 rounded-md w-full ">

            <label for="username" class="block mt-4 text-sm font-medium text-zinc-300">Username</label>
            <input type="text" id="username" name="username" value="<%= user.username %>" class="mt-1 p-2 rounded-md w-full">

            <button type="submit" class="mt-5 px-5 py-2 rounded-lg bg-green-500 text-xs md:text-base font-semibold">Save Changes</button>
        </form>
    </div>

</body>
</html>

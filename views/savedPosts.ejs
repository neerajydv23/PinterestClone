<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Saved Posts</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-sans bg-zinc-800 text-white">

    <!-- nav bar -->
    <div class="w-full px-4 sm:px-6 md:px-8 lg:px-10 py-3 flex items-center justify-between bg-zinc-800 text-white">
        <a href="/feed" class="text-lg font-semibold flex items-center gap-2">
            <img class="w-8 h-8 md:w-10 md:h-10"
                src="https://cdn.icon-icons.com/icons2/2992/PNG/512/pinterest_logo_icon_187314.png" alt="">
            <span class="hidden sm:inline">Pinterest</span>
        </a>
        <div class="flex items-center">
            <a href="/search"><input class="p-2 rounded-lg w-28 md:w-40 lg:w-60 ml-2 md:ml-4 mr-2 md:mr-4" type="text"
                    placeholder="Search"></a>
            <a href="/profile" class="text-sm md:text-base mr-2">Profile</a>
            <a class="px-2 py-1 md:px-3 md:py-2 bg-red-700 text-white rounded-lg text-xs md:text-sm" href="/logout">Log
                Out</a>
        </div>
    </div>

    <!-- <div class="w-full min-h-screen px-4 sm:px-6 md:px-8 lg:px-10 py-8 text-white">
        <div class="cards flex flex-wrap -mx-4 justify-center">
          <%  if(loggedInUser.saves.length>0){ %>
            <% loggedInUser.saves.forEach(function(savedPostId) { %>
                <div class="card w-full sm:w-1/2 md:w-1/3 lg:w-1/4 xl:w-1/5 px-4 mb-8">
                    <a href="/post/<%= savedPostId._id %>">
                        <div class="w-full h-48 sm:h-64 bg-zinc-700 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover" src="/images/uploads/<%= savedPostId.image %>" alt="">
                        </div>
                        <h5 class="mt-2 text-sm text-white">
                            <%= savedPostId.title %>
                        </h5>
                    </a>
                </div>
            <% }) %>
            <% } %>
        </div>
    </div> -->

</body>

</html>
